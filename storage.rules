rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /images/{userId}/{imageId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    match /feedback_images/{feedbackId} {
      allow read: if true;
      allow create: if request.resource.size < 10485760
        && request.resource.contentType.matches('image/.*');
      allow update, delete: if false;
    }
    match /chat_attachments/{feedbackId}/{fileName} {
      allow read: if true;
      allow create: if request.resource.size < 20971520; // 20MB
      allow update, delete: if false;
    }
  }
}
